# kondate-app 引き継ぎメモ（PC移行用）

最終更新: 2026-09-17

## このアプリは何をするものか

家庭の食材在庫を管理し、消費期限が近いものを優先的に使うレシピを提案する、
個人・家族向けの献立サポートWebアプリ。

想定フロー：
1. 食材・調味料の在庫を登録（賞味期限つき）
2. カレンダーの日付ごとにレシピを提案してもらい、「作る」で登録
3. 登録時、使う食材の残量を入力（食材）／使い切るかチェック（調味料）
4. 不足している食材・調味料は確認のうえ買い物リストへ
5. 買い物リストの「購入」から在庫に反映 → 1に戻る

無料・サーバーレスで運用する方針（サーバー費用ゼロ、個人の趣味プロジェクト）。

## 現在実装済みの機能

- **食材・調味料の在庫管理**（`js/pages/ingredients.js`）
  - 「食材」「調味料」タブで登録欄・一覧を分離
  - 食材：名前・数量・単位・消費期限を登録。同名×同単位×同賞味期限のロットは自動で数量統合
  - 調味料：名前のみで登録（数量管理はせず「あるか/ないか」のみ。同名の重複登録は不可）
  - 食材一覧は同名ごとに1項目へ集約し、賞味期限違いのロットを内訳表示
- **献立提案（カレンダー形式）**（`js/pages/suggest.js` + `js/suggest.js`）
  - 週表示カレンダーで日付を選び、その日に複数レシピを登録可能
  - コース種別（主菜／副菜／汁物／主食／デザート／すべて）で絞り込み
  - 在庫の消費期限・家族の好み・アレルギー・「食べたいもの」リクエスト・直近の献立履歴からスコアリングして提案
  - 「レシピを再検索する」で次の候補を表示（全候補を5件ずつページング）
  - 「作る」時：食材は残量入力（空欄＝使い切り）、調味料は使い切るものにチェック。不足分・使い切る調味料は確認ダイアログを経て買い物リストに追加
  - 「予定を取り消す」で在庫・買い物リストを作る前の状態に復元（スナップショットベース）
- **買い物リスト**（`js/pages/shopping.js`）
  - 手動追加、「食べたいもの」からの不足食材自動抽出
  - 「購入」で在庫に反映（食材は数量・単位・消費期限を入力、調味料は名前のみで即反映）、「削除」は買わずにリストから消すだけ
- **食べたいものリクエスト**（`js/pages/requests.js`）：自由文＋ジャンルで登録、提案のスコアリングに加点
- **家族・好みプロフィール**（`js/pages/profile.js`）：家族構成（人数×食べる量で人前数を算出）、アレルギー、苦手食材、好みジャンルを登録。分量は`js/quantity.js`でレシピ基準人数からスケーリング表示
  - **データのバックアップ**：全データをJSONファイルにエクスポート／別ブラウザへインポート可能（機種変更・プライベートモード対策）
- **レシピデータ**：`data/recipes.json` に293件（主菜140／副菜67／主食44／デザート24／汁物18）。楽天レシピAPI（Rakuten Recipe Web Service）で人気ランキングを一度だけ参照し、タイトル・写真・元URLは実データ、材料の分量・手順はこのアプリ用に書き起こしたオリジナル。ライブAPI呼び出しは行っていない（`js/recipes.js`は同梱JSONを読むだけ）

## 未完了のタスク・既知の問題

- **自動テストなし**。動作確認はブラウザでの手動確認のみ
- **バックエンド・アカウント機能なし**。全データは各端末のブラウザのlocalStorageに保存されるだけで、複数端末間の同期は不可（意図的な設計。ユーザーとパートナーはそれぞれ別端末で使う前提で運用中）
- **アクセス制御なし**。GitHub Pagesの公開URLを知っていれば誰でも開ける（ログイン機構は無い）
- **レシピ件数に偏りあり**：汁物18件・デザート24件と少なめ。追加収集する場合は楽天Recipe APIの再利用が必要（後述）
- **調味料キーワードの網羅漏れ**：`js/categoryInference.js`のキーワード一致に無いもの（例：市販の「〇〇のタレ」等）は「その他」カテゴリになる
- **キャッシュ対策が手動運用**：`index.html`の`style.css?v=N`・`js/app.js?v=N`のクエリを、CSSやJSを変更するたびに手動でインクリメントする運用にしている（現在 style.css=v4, app.js=v3）。上げ忘れるとブラウザキャッシュで変更が反映されないことがある
- localStorageのスキーマを過去に何度か変更している（例：`history`→`mealPlans`へのリネーム）ため、**データ移行の仕組みは無い**。ストレージのキー構造を変える場合は、既存ユーザーの保存データが読めなくなる点に注意

## ローカルでの起動手順

前提：Python 3系がインストールされていること（`python`または`python3`コマンド）。ビルド不要・依存パッケージ無しの素のHTML/CSS/JSアプリ。

1. リポジトリのルート（`kondate-app`フォルダ）で以下を実行
   ```
   python serve.py 5173
   ```
   （`serve.py`はキャッシュを無効化するヘッダー付きの簡易HTTPサーバー）
2. ブラウザで `http://localhost:5173` を開く
3. Windowsでは `起動.bat` をダブルクリックしてもよい（サーバー起動＋ブラウザを自動で開く）

本番は GitHub Pages（`https://syodai3386-cloud.github.io/kondate-app/`）でmainブランチから配信。`git push`すれば自動反映される（反映まで数分かかることがある）。

## 使用している技術・主要ファイルの役割

- **技術**：素のHTML/CSS/JavaScript（ESモジュール）、ビルドツールなし、フレームワークなし。永続化はブラウザの`localStorage`のみ
- **ルーティング**：`location.hash`によるSPA的な画面切り替え（`js/app.js`）

| ファイル | 役割 |
|---|---|
| `index.html` | エントリーポイント。ナビと`#app`コンテナ、CSS/JS読み込み |
| `style.css` | 全ページ共通スタイル |
| `js/app.js` | ルーティング（ハッシュ監視）とナビゲーション描画 |
| `js/storage.js` | localStorageの読み書き窓口。食材データの正規化（同名・同条件ロットの自動統合）もここで実施。バックアップ用の`exportAll`/`importAll`もここ |
| `js/recipes.js` | `data/recipes.json`を取得（キャッシュ付き） |
| `js/suggest.js` | レシピ提案のスコアリングロジック（期限切迫度・好み・リクエスト・ジャンルの偏り） |
| `js/quantity.js` | 家族構成から目安人数を算出し、材料分量をスケーリング |
| `js/textMatch.js` | 食材名・リクエスト文の緩い一致判定（部分一致ベース） |
| `js/categoryInference.js` | 食材名からカテゴリ（肉／魚／野菜／調味料／果物など）を推測するルール |
| `js/pages/*.js` | 各画面（食材／献立提案／食べたいもの／買い物リスト／家族・好み）の描画とイベント処理 |
| `data/recipes.json` | レシピ本体データ（293件） |
| `serve.py` | ローカル確認用の簡易HTTPサーバー（no-cacheヘッダー付き） |
| `起動.bat` | Windows用のワンクリック起動スクリプト |

## 新PCで別途用意・設定が必要なもの

- **Python 3系**（ローカルでの動作確認用サーバーに必要。アプリ自体の実行には不要＝GitHub Pages上では無関係）
- **GitHubアカウントとこのリポジトリへのpush権限**（リモート: `https://github.com/syodai3386-cloud/kondate-app.git`）
- **（レシピを追加収集する場合のみ）Rakuten Developersのアプリ登録**：Application ID・Access Keyが必要。これらはセキュリティ上コード・リポジトリのどこにも保存していないため、必要になった際に改めて用意する
- 環境変数：**このプロジェクトは環境変数を使用していない**（ビルド無しの静的サイトのため、`.env`等の仕組み自体が存在しない）

## 開発を続ける際の注意点

- **CSS/JSを変更したら`index.html`のキャッシュ対策クエリ（`?v=N`）を必ず上げる**。忘れると本人だけでなく利用者側で変更が反映されずに見える
- ブラウザのネイティブフォーム部品（特に`<input type="date">`）はブラウザごとに描画上の癖があるため、見た目を統一するCSSを書いたらChromeだけでなく実機のSafari（iPhone）でも確認するのが望ましい。今回は`height`固定ではなく`min-height`で吸収する方式にしている（`style.css`の`.field-group input`まわり参照）
- Rakuten APIの認証情報は**絶対にコード・コミットに含めない**（これまでも徹底してきた方針）
- `.form-row`はflexで`align-items`未指定（stretch）なので、ラベル付き項目（`.field-group`）と裸のボタン等を同じ行に混在させると意図せず引き伸ばされる。混在させる場合は`align-self`指定を忘れない
- localStorageの構造を変える変更をするときは、既存データが読めなくなる/消えたように見えるリスクがあることを念頭に置く（マイグレーション機構が無いため）
- ユーザーはプライベートブラウジングモードと通常モードでデータが分離されることに一度混乱した経緯があるため、通常モードでの利用を推奨している旨は伝達済み
